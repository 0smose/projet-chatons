<div class="col col-8 mx-auto text-dark">
	<h2 class="mt-3"><%= @item.title %></h2>

	<img src="<%= @item.image_url %>" alt="picture" class="col mx-auto" >
	
	<ul class="list-group list-group-flush mb-3 mt-3">
		<li class="list-group-item"><%=@item.description %></li>
		<li class="list-group-item"><%=@item.price %> €</li>
	</ul>
	<%= link_to "Edit", edit_admins_item_path(@item.id), class: "btn btn-primary" %>
	<%= link_to 'Delete', admins_item_path(@item.id), method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger ml-3" %>
</div>