<div class="container-fluid">
	<div class="row">
		<!-- Dashboard Menu -->
		<nav class="col-md-2 d-none d-md-block bg-light sidebar">
			<div class="sidebar-sticky">
				<ul class="nav flex-column">
					<li class="nav-item">
						<h6 class="sidebar-heading d-flex justify-content-between align-items-center px-3 mt-4 mb-1 text-muted">
							<span>Dashboard</span>
						</h6>
						<%=link_to "Home", admins_path, class: "nav-link active" %>  
					</li>
					<li class="nav-item">
						<%=link_to "Items", admins_items_path, class: "nav-link active" %> 
					</li>
					<li class="nav-item">
						<%=link_to "Users", admins_users_path, class: "nav-link active" %> 
					</li>
				</ul>
			</div>
		</nav>

		<main role="main" class="col-md-9 ml-sm-auto col-lg-10 pt-3 px-4">
			<div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pb-2 mb-3 border-bottom">
				<h2 class="text-center mt-4 mb-4"> Items </h2><%= link_to "Create item", new_admins_item_path, class: "btn btn-success btn-sm ml-3 mt-4" %><hr>
			</div>
			<div class="container">
				
				<div class="row">

					<!-- Start of the items loop -->
					<% @items.order(created_at: :desc).each do |item| %>
					<!-- For each item, we create a column and a card -->
					<div class="col col-4 mt-5">
						<div class="card shadow mb-3">
							<!-- Item title -->
							<h5 class="card-header"><%= item.title.truncate(30) %></h5>
							<!-- Item image -->
							<img class="card-img-top" height="200px" src="<%= item.image_url %>" alt="Card image cap">
							<div class="card-body">
								<!-- Item description -->
								<p class="card-text"><%= item.description %></p>
								<!-- Button to item -->
								<%= link_to "See item", admins_item_path(item.id), class: "btn btn-primary btn-sm" %>
							</div>
						</div>
					</div>
					<% end %>
				</div>
			</div>
		</main>
	</div>
</div>
